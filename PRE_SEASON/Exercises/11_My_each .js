/*
    Description

    Time to print each elements of an array.

    Create a function my_each which receives an array of strings as parameter and iterate over the array and use a function which print for each value.
    This function return nothing.

*/

function my_each(param_1) {
  param_1.forEach((element) => console.log(element));
}
