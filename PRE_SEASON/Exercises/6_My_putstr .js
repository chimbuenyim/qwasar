/*
    Description

    Create a function that displays a string of characters on the standard output.
    The address of the string's first character is in the pointer entered as parameter in the function.
*/

function my_putstr(param_1) {
  process.stdout.write(param_1);
}
